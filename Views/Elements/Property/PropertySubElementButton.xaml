<UserControl x:Class="MapEditor.Views.Elements.PropertySubElementButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="20"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid x:Name="MainGrid" Margin="0 10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock TextAlignment="Left" VerticalAlignment="Center" x:Name="NameTextBlock"
                   Text="Property with button" Margin="20,0,20,0" TextWrapping="Wrap"/>

        <Button Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" x:Name="ActionButton"
                VerticalContentAlignment="Center" Content="Action" Margin="20,0,20,0"
                Style="{DynamicResource MaterialDesignOutlinedButton}" FontSize="20"/>
    </Grid>
</UserControl>
