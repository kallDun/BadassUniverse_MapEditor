<UserControl x:Class="BadassUniverse_MapEditor.Views.Menu.WorldItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="20"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d">
    <Button Height="45" Padding="4" Style="{DynamicResource MaterialDesignOutlinedDarkButton}"
            x:Name="MainButton">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="World Name" x:Name="WorldNameTextBlock"/>
            <TextBlock Text="Id = 5" x:Name="WorldIdTextBlock"/>
        </StackPanel>
    </Button>
</UserControl>
