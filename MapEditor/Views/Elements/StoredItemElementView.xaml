<UserControl x:Class="BadassUniverse_MapEditor.Views.Elements.StoredItemElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             TextElement.FontWeight="Regular"
             TextElement.FontSize="20"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Width="auto" Margin="0 10" x:Name="MainGrid" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Image x:Name="ItemIcon" Source="/Assets/Icons/Room.png" Width="40" Height="40" 
               HorizontalAlignment="Left" VerticalAlignment="Center"
               Margin="20 0 20 0"/>

        <StackPanel Grid.Column="1" Margin="0 0 10 0" VerticalAlignment="Center">
            <TextBlock x:Name="ItemName" Text="Item Long Name" FontSize="18" Margin="0 0 0 3" TextWrapping="Wrap"/>
            <TextBlock x:Name="IdTextBlock" Text="ID: 0" FontSize="15" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
